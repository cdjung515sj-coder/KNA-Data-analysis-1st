# -*- coding: utf-8 -*-
"""
[11] 결측치 실수 / 주의사항 모음

1) NaN을 == 로 찾지 않기
X  df["온도"] == NaN
O  df["온도"].isna()

2) 0과 NaN 구분
0   = 실제값일 수 있음
NaN = 값 없음

3) isna()와 isna().sum() 구분
isna()       -> 위치
isna().sum() -> 개수

4) head()만 보고 결측 없다고 판단하지 않기
전체는 isna().sum()으로 확인

5) dropna()는 NaN 칸만 삭제하는 것이 아님
기본은 결측 있는 행 전체 삭제

6) dropna() 결과 저장
clean = df.dropna()

7) thresh 의미 주의
thresh=N
= 정상값 최소 N개 이상이면 유지

8) 센서 NaN을 0으로 무조건 채우지 않기
통계 왜곡 가능

9) 평균만 무조건 쓰지 않기
이상치/치우침이 있으면 중앙값 고려

10) 범주형은 최빈값 고려
mode()

11) ffill 전에 정렬

12) 서로 다른 설비를 섞어 ffill하지 않기

13) 처리 후 재확인
shape
isna().sum()
isna().sum().sum()

14) 위장 결측 놓치지 않기
0 / -999 / 999 / "" / 공백 등
"""

print("이 파일은 결측치 처리 시 자주 하는 실수를 빠르게 확인하는 요약 파일입니다.")
