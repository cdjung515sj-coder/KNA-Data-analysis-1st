# 실습 6. describe로 이상 신호 찾기
# 평균 / 분위수 / 최대를 읽어 이상 신호 있는 열 찾기

# 12_metro_compressor.csv
# 온도 / 진동의 이상값 존재

import pandas as pd

df_metro_compressor = pd.read_csv("data/12_metro_compressor.csv")
print(df_metro_compressor.shape)
print(df_metro_compressor.head())
print(df_metro_compressor.tail())

df_metro_compressor.info()

# STEP 1 : describe 후 75%와 max 차이 큰 열 찾기
print(df_metro_compressor.describe())


# 1 온도의 평균과 최댓값 차이를 숫자로 적었는가
# 평균 75 대 max 75.0 — 차이를 기록
oil_temp_max = 75.000000 
oil_temp_min = 63.181910 
temp_diff = oil_temp_max - oil_temp_min  # 11.8181
print(f"온도의 평균과 최댓값 차이 : {temp_diff:.4f}")

# 2 75%와 max 차이가 큰 열을 두 개 이상 찾았는가
# 온도와 진동 — max가 멀리 튄 열 찾기
# 해당 열을 찾아 주석으로 적어줘
# 1. 오일온도 : 75%(68.10℃) 대비 max(75.00℃)가 6.9℃ 높음 (상위 구간에서 열 누적으로 인한 과열 징후)
# 2. 배출압력 : 75%(-0.02) 대비 max(0.60)가 크게 튐 (표준편차 0.047 대비 격차가 매우 큰 돌발 스파이크 이상치)


# 3 모터전류처럼 고른 열과 비교해 차이를 설명
# 모터전류는 75%와 max가 가까움 - 온도와의 차이 설명 서술
# - 75% 지점(68.100000) 대비 최댓값(75.000000)의 차이 = 6.900000
# - 평균(63.18) 및 중앙값(62.90) 대비 상위 구간에서 수치가 급격히 상승함.



# ============================================================================================================

# 실습 7. 통계량 문장으로 묘사
# describe 통계를 자기 말로 풀어 설명

# 설비 센서 데이터의 "한 열(1 column)"을 묘사

import pandas as pd

df_metro_compressor = pd.read_csv("data/12_metro_compressor.csv")
df_metro_compressor.info()

print(
    # df_metro_compressor[4]
)  # 특정 번호를 쓰기보단 판다스에서 컬럼만 따로 빼줌 넘파이와는 다르게 추가해줌 그래서 밑에처럼 사용해

print(df_metro_compressor["오일온도"])
df_metro_compressor.info()
df_metro_compressor["오일온도"].info()

#
# 오일 온도 컬럼만 떼서 describe 통계 보기
print(df_metro_compressor["오일온도"].describe())

# count    199.000000
# mean      63.181910
# std        6.249822
# min       50.100000
# 25%       58.100000
# 50%       62.900000
# 75%       68.100000
# max       75.000000
# Name: 오일온도, dtype: float64


# 1 평균·min·max·중앙값을 문장에 정확히 넣었는가
# 이 값은 보통 OO 정도이고, 가장 낮을 때 OO, 높을 때 OO 형식

# 이 값은 보통 63.181910 정도이고, 가장 낮을 때는 50.100000, 높을 때는 75.000000 이다.



# 2 표준편차를 보고 안정성을 판단했는가
# 표준편차로 안정적인지 들쭉날쭉한지 판단 (판단은 AI를 활용해도 괜찮음)
# std        6.249822
# 오일온도의 표준편차 6.25 는 평균63.18 대비 약 9.89% 수준(변동계수 CV)으로, 통계적인 수치만 놓고 보면 전체 데이터 분포는 대체로 안정적인 편에 속합니다.



# 3 숫자를 그냥 옮기지 않고 의미로 풀어 썼는가
# 숫자만 옮기지 말고 의미로 풀어 쓰기

# 1) 모터전류 (안정적인 제어 상태):
#    - 대기 상태의 낮은 전류(0.04A)부터 가동 시의 높은 전류(6.19A)까지 수치가 단계적으로 균일하게 상승합니다.
#    - 이는 모터가 갑작스러운 충격 없이 제어 기준 안에서 규칙적이고 예측 가능하게 동작하고 있음을 뜻합니다.

# 2) 배출압력 (돌발 이상 신호 / 센서 튐 현상):
#    - 전체 운전 시간의 75% 이상 동안 압력이 -0.02로 일정하게 고정되어 있다가, 특정 순간에만 0.60으로 급격히 치솟습니다.
#    - 완만한 변화가 아닌 순간적 폭증이므로, 정상적인 토출 과정이 아니라 토출 밸브의 일시적 막힘, 
#      배관 유체 충격(Surge), 또는 센서의 순간 노이즈와 같은 '돌발 이상 징후'로 해석됩니다.

# 3) 오일온도 (열 누적 및 과열 징후):
#    - 중앙값(62.9℃)과 상위 75%(68.1℃)까지는 완만하게 상승하다가 최댓값(75.0℃) 구간에서 격차가 크게 벌어집니다.
#    - 이는 설비가 장시간 연속 가동되면서 냉각 효율이 떨어지거나, 일시적 과부하로 인해 내부 열이 
#      응축되어 나타난 '과열(Overheating) 징후'를 의미합니다.
