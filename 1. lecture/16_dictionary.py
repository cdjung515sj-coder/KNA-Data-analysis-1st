# 리스트로 크루 여러분의 이름을 나열해봅시다
data_class = {"수진", "준원", "재홍", "쿼카"}

# 딕셔너리로 정확하게 역할까지 부여해봅시다. 스네이크타입 이름으로 해줘~ _
data_class_dict = {"반장": "준원", "부반장": "재홍", "공주": "수진", "귀여운": "쿼카"}

# 센서로 부터 얻는 예시 데이터로 딕셔너리를 만들어봅시다
sensors = {"센서이름": "보일러", "모터온도": 78, "진동": 0.5}

# 번역 방식의 차이: C/Java는 책 전체를 번역하듯 한 번에 고쳐야 하지만, 파이썬은 동시통역처럼 코드를 한 줄씩 즉시 실행합니다.

# 잦은 수정에 유연함: 데이터 항목이 추가·삭제되며 바뀌어도 파이썬은 바로바로 고쳐서 결과를 검증하기에 훨씬 유리합니다.

# 학습 이유: 파이썬은 단순히 쉬워서가 아니라, 변화가 많은 데이터 분석 환경 및 목적에 가장 최적화된 언어이기 때문입니다.

print(sensors)  # {'센서이름': '보일러', '모터온도': 78, '진동': 0.5}
print(type(sensors))  # <class 'dict'>
empty = {}  # 빈 딕셔너리 생성
print(type(empty))  # <class 'dict'>

# 키로 값 꺼내기
sensors = {"센서이름": "보일러", "모터온도": 78, "진동": 0.5}
print(sensors["모터온도"])  # ""포함해야돼 !
# print(sensors[모터온도]) # NameError: name '모터온도' is not defined
print(sensors["진동"])
# print(sensors[]) # SyntaxError: invalid syntax. Perhaps you forgot a comma?
# print(sensors([78])) # TypeError: 'dict' object is not callable
# 없는 키는 KeyError


# 새로운 키 , 값 추가하기 =============================================
# 딕셔너리[ 새 키 ] = 값
# # 1. 딕셔너리에 없는 키를 그냥 호출하면? (에러 발생)
# 존재하지 않는 키를 dict['key'] 방식으로 직접 접근하면 KeyError가 발생합니다.

# 비어있는 값(없는 값)을 안전하게 가져오는 방법:

# get() 메서드 사용: 없는 키를 요청해도 에러 없이 None(또는 지정한 기본값)을 반환합니다.

# # 2. 센서 데이터처럼 실시간으로 데이터를 추가하려면?
# 실시간 센서나 외부 데이터는 미리 구조를 fixed(고정) 해둘 수 없기 때문에 동적 추가가 필수적입니다.

# 새로운 키와 값을 추가하는 방법:

# 존재하지 않는 '새로운 키를 언급'하면서 값을 할당하면 딕셔너리에 자동으로 새 항목이 추가됩니다.

# # 3. 이미 있는 키 vs 없는 키
# 키가 이미 있는 경우: 기존 값이 새 값으로 변경(업데이트)됩니다.

# 키가 없는 경우: 딕셔너리에 새로운 키-값 쌍이 추가됩니다.

# 💡 한 줄 결론

# 고정된 형태가 없는 실시간 데이터는 새로운 키를 언급하여 값을 할당하는 방식으로 딕셔너리를 자유롭게 확장할 수 있습니다.

# 오른쪽에서 읽어봐 언급이 되어있어 등호를 왼쪽에 집어넣는다 라고 생각하면 돼

print(sensors["센서이름"])
print(sensors["모터온도"])
print(sensors["진동"])

# 기존에 있던 key의 값을 변경
sensors["센서이름"] = "펌프"  # 센서이름 값 변경
sensors["진동"] = 0.7

print(sensors)  # 보일러 -> 펌프로 이름 변경됨.
#               {'센서이름': '펌프', '모터온도': 78, '진동': 0.5}


# 기존에 없던 key 의 값을 추가
sensors["펌프압력"] = 95
# {'센서이름': '펌프', '모터온도': 78, '진동': 0.5, '펌프압력': 95}
sensors["유량"] = 42
# {'센서이름': '펌프', '모터온도': 78, '진동': 0.5, '펌프압력': 95, '유량': 42}

# ⚠️ 똑같은 키 이름을 꼭 언급해줘야 해
print(sensors)


# 기존 값 수정하고 삭제하기 ============================================================
# del
# 딕셔너리 [있는 키] = 새값
# del 딕셔너리 [키]

# 더 이상 필요없는 key와 크 value 삭제하기
del sensors["모터온도"]  # {'센서이름': '펌프', '진동': 0.7, '펌프압력': 95, '유량': 42}

print(sensors)

# print(sensors["모터온도"])  # 더이상 없는 key를 호출하면 에러 발생 !!
# ⚠️ KeyError: '모터온도'

# get 으로 없는 키 안전하게 다루기 =======================================================
# get은 없는 키를 꺼내도 오류 대신 기본값을 돌려줌
# 딕셔너리.get( 키 , 기본값 )

print(sensors.get("센서이름"))
print(
    sensors.get("모터온도")
)  # None # 더이상 없는 키 값을 호출하면 에러가 발생했는데 None이라고 알려줌 😜


# motor_ 에는 숫자가 담길거라 생각했지만
motor_degree = sensors.get("모터온도", 0)

# 대체값으로 0 으로 넣는다고 하는데 케바케로 상황에 따라 달라짐

# motor_degree에 숫자가 안담기면 에러 발생
next_degree = motor_degree + 10
print(next_degree)

is_motor_degree_key = "센서이름" in sensors
# 등호를 중심으로 오른쪽 먼저 보셔라
# 센서스 딕서녀리 안에 "모터온도가" 있느냐~

print(is_motor_degree_key)  # => 있으면 True / 없으면 False

if is_motor_degree_key:
    print("그런 키 있어요!")
else:
    print("그런 키 없어요!")

# 위 코드는 이렇게 보통 이렇게 쓰인다
if "센서이름" in sensors:
    print("그런 키 있어요!")
else:
    print("그런 키 없어요!")

# 딕셔너리 키가 될 수 있는것
# 문자열 가능 안 바뀜 — "모터온도" 같이 한 번 정해지면 변경 불가
# 숫자 가능 안 바뀜 — 정수·실수 모두 키로 사용 가능
# 튜플 가능 안 바뀜 — 앞서 튜플이 키 될 수 있던 이유

# 리스트 불가 바뀜 — 이사 간 집으로 편지를 못 보내는 것과 같음
# # 리스트([])는 언제든지 요소를 추가(append), 삭제, 변경할 수 있는 가변(Mutable) 데이터 구조입니다.
# 내용물이 바뀌면 주소가 꼬여버리기 때문에 파이썬에서는 리스트를 딕셔너리의 키로 절대 사용할 수 없게 막아 두었습니다. (사용 시 TypeError 발생)

# ===========================================================================
print("=================== 순회와 갱신 ======================")
print("------- .keys() / .values() --------")

# keys를 가져와보자 !
print(sensors.keys())  # dict_keys(['센서이름', '진동', '펌프압력', '유량'])

# values를 가져와보자 !
print(sensors.values())  # dict_values(['펌프', 0.7, 95, 42])

# len을 통해 몇개의 key-value 조합들이 있는지 보자
print(len(sensors))  # 4


# ---------------------------------------------------------------------------
print(
    "-------------- .items() 반복문에서 사용하며 키와 같은 짝으로 함께 꺼내기 -------------------"
)

# 이런 경우는 많이 있으므로 알아둬 데이터가 부족해도 너무 많아도 문제가 되니까 이런건 중요하죠
if len(sensors) < 5:
    print("내용이 부족해요!")

for key, value in sensors.items():
    print(key)
    print(value)

# 위와같이 사용하기 보단, 의미있는 이름으로 사용할 것!
for name, value in sensors.items():
    print(name)
    print(value)

# ================================================================================
# ================================================================================

# 재밌는 사례를 추가로 만들어보제이!

# 나라 이름으로 딕셔너리로 만들어봅시다.
# 유럽 = 스페인(ESP), 프랑스(FRA), 독일(GER), 스위스(SUI), 네덜란드(NED)
# 아시아 = 한국(KOR), 일본(JPN), 중국(CHN), 사우디(SAU), 이란(IRN)
# 남미 = 아르헨티나(ARG), 브라질(BRA), 칠레(CHI), 콜롬비아(COL), 우루과이(URU)

korea = {"국가명": "대한민국", "약칭": "KOR"}
japan = {"국가명": "일본", "약칭": "JPN"}

# 아시아 나라들을 하나의 리스트로 모아봅시다
asia = [korea, japan]
print(asia)

# 유럽 나라들을 하나의 리스트로 모아봅시다

# 여러분의 조별과제
# 포켓몬 1,2,3, 진화단계들을 딕셔너리로 만들고
# 그 포켓몬 딕셔너리들이 모인 배열을 만들어봅시다
# 그 배열 데이터를 화면에 print 합니다
# 가능하면 그 배열의 데이터들을 for-in을 사용해서 하나씩 꺼내 print 합시다
# 다 되면 불러주세요~

pokemon_evolution_list = [
    {"기본형": "이상해씨", "1차진화": "이상해풀", "2차진화": "이상해꽃"},
    {"기본형": "파이리", "1차진화": "리자드", "2차진화": "리자몽"},
    {"기본형": "꼬북이", "1차진화": "어니부기", "2차진화": "거북왕"},
    {"기본형": "캐터피", "1차진화": "단데기", "2차진화": "버터플"},
    {"기본형": "뿔충이", "1차진화": "딱충이", "2차진화": "독침붕"},
    {"기본형": "구구", "1차진화": "피죤", "2차진화": "피죤투"},
    {"기본형": "뚜벅초", "1차진화": "냄새꼬", "2차진화": "라플레시아"},
    {"기본형": "발챙이", "1차진화": "수륙챙이", "2차진화": "강챙이"},
    {"기본형": "케이시", "1차진화": "윤겔라", "2차진화": "후딘"},
    {"기본형": "알통몬", "1차진화": "근육몬", "2차진화": "괴력몬"},
    {"기본형": "모다피", "1차진화": "우츠동", "2차진화": "우츠보트"},
    {"기본형": "꼬마돌", "1차진화": "데구리", "2차진화": "딱구리"},
    {"기본형": "고오스", "1차진화": "고우스트", "2차진화": "팬텀"},
    {"기본형": "미뇽", "1차진화": "신뇽", "2차진화": "망나뇽"},
]

if len(pokemon_evolution_list) >= 10:
    print("내용이 충분해요!")


for pokemon in pokemon_evolution_list:
    normal, first, second = pokemon.values()
    print(f"1차진화: {normal}, 2차진화: {first}, 3차진화: {second}")

print("===========================")

for pokemon in pokemon_evolution_list:
    print(pokemon.get("파이리"))

print("===========================")

for pokemon in pokemon_evolution_list:
    # pokemon.items()는 ('기본형', '이상해씨'), ('1차진화', '이상해풀')... 을 반환
    for key, value in pokemon.items():
        print(f"{key}: {value}", end=" / ")
    print()  # 줄바꿈


print("===========================")
# pokemon_name = input(
#     "다음 포켓몬 중 하나를 입력\n[구구], [뚜벅초], [발챙이], [케이시], [알통몬], [모다피], [꼬마돌], [고오스], [미뇽]\n포켓몬 이름 입력: "
# )

# for pokemon in pokemon_evolution_list:
#     if pokemon["기본형"] == pokemon_name:
#         print(
#             f"[{pokemon['기본형']}] 1차진화 : {pokemon['1차진화']}, 2차진화 : {pokemon['2차진화']}"
#         )
#         break
# else:
#     print("포켓몬 이름을 확인해주세요")


# ==================================================================
print(" 임계값 비교 ======================================")

# 두 딕셔너리를 key-value 조합으로 하나씩 꺼내어 비교하기!

# 전제조건) 다음에 두 딕셔너리는 같은 key 들을 가지고 있습니다.
# 실제 데이터
values = {"모터온도": 95, "압력": 88}

# 임계치 데이터
limits = {"모터온도": 90, "압력": 90}

# if values > limits:
#     print("임계치 초과!")
#     # 문제가 발생하므로 키들을 하나씩 꺼내오면서 비교하는게 원칙임

for name, value in values.items():
    # 찰 돌아가는지 보려면 print 문으로 계속 실행해보기
    print(f"{name} : {value}")

    # limits 딕셔너리에도 name의 key가 있다면, 가져와서 비교하자!
    # limits[key]: 키가 없으면 에러(KeyError)가 터져 프로그램이 죽음
    # if value > limits[name]:
    #     print(name, "경고")

    # get 을 사용해서 하는것을 추천합니다. 측정 데이터 임계치 데이터 사이에서 어긋나면 대응을 해줘야 하니까
    # limits.get(key): 키가 없어도 에러 없이 안전하게 넘어가기 위해 사용
    if value > limits.get(name, 0):
        print(name, "경고")

        # 주의점: limits.get(name, 0)처럼 기본값으로 0을 넣으면, 기준 없는 항목이 의도치 않게 경고 처리될 수 있으므로 None 체크를 해주는 것이 안전함

# ==================================================================
print(" update로 여러 값 한 번에 갱신하기 ======================================")

sensors = {"모터온도": 78, "진동": 0.5}
new_data = {"모터온도": 80, "유량": 42}
sensors.update(new_data)  # 기존 딕셔너리에 새로운 딕셔너리의 key-value 조합 추가
print(sensors)  # 결과 : {'모터온도': 80, '진동': 0.5, '유량': 42}
# 실제로 많이 쓰이지 않음 왜냐하면 실수가 많음. 센서 데이터들 키마다, 밸류마다 변화해야되는데 update는 덮어씌움
# 예측불허일 떄도 있음. 그래서 제어가 가능한 그림이 가능하면 가능하지만, 많은 계측 데이터들은 키들이 많아서 이걸 사용한다면 문제 발생 여부가 높음
# 가급적 update를 피하는 것도 방법. 이런 메서드가 존재한다만 알아줘


# ==================================================================
print(" zip으로 두 리스트 딕셔너리 만들기 ======================================")
# dict(zip(이름, 값))
# 이름 리스트와 값 리스트를 짝지어 딕셔너리로 묶음.
# 지퍼처럼 두 리스트를 앞에서부터 하나씩 짝지음

# zip으로 key들의 배열과 value들의 배열을 묶어서 새로운 딕셔너리를 만들 수 있습니다.
names = ["모터온도", "진동", "압력"]
values = [78, 0.5, 95]
sensors = dict(
    zip(names, values)
)  # zip이라는 기능으로 두 배열을 사용해 묶고 dict 타입 딕셔너리로 만든다.
# ⚠️⚠️ key들과 value들의 숫자를 맞추는 것부터 잘 해야함 !!
print(sensors)  # {'모터온도': 78, '진동': 0.5, '압력': 95}


# 사람이 실수 할 수 있는 경우가 많아서 선호하지 않아요

# 안좋은 케이스 보여줄게⬇️
# names = ["모터온도", "진동", "압력", "유량"]
# values = [78, 0.5, 95]
# sensors = dict(zip(names, values))
# print(sensors) # 유량이 유실이 돼.....

# 딕셔너리 안에 value로 리스트도 가능함
idols = {
    "BTS": ["RM", "진", "슈가", "제이홉", "지민", "뷔", "정국"],
    "블랙핑크": ["지수", "제니", "로제", "리사"],
    "뉴진스": ["민지", "하니", "다니엘", "혜린", "혜인"],
}

my_classroom = [
    {"학년": 3, "반": 1, "반장": "홍길동", "부반장": ["고길동", "둘리"]},
    {"학년": 3, "반": 2, "반장": "메타몽", "부반장": ["고라파덕", "피카츄"]},
    {"학년": 3, "반": 3, "반장": "정준원", "부반장": ["차은우", "안재홍"]},
]
