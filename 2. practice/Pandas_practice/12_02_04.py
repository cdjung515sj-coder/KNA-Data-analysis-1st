# 실습 4. 열 이름, 자료형 점검
# 자료형이 의도와 맞는지 판단해보자
# 12_metro_compressor.csv 데이터 사용
#

import pandas as pd

df_metro_compressor = pd.read_csv("data/12_metro_compressor.csv")

print(df_metro_compressor.dtypes)  # 열이 의도와 맞는지 판단

# 1. 숫자로 계산할 열이 모두 숫자 자료형인가
# - 압축압력, 배출압력, 저장압력, 오일온도, 모터전류 열 모두 'float64'
# - 통계 계산(평균, 최댓값 등) 가능

# 2. ID 등 글자 열이 글자 자료형인가
# - 시간 식별자인 '측정시각'과 상태 분류값인 '가동상태' 열이 'object'(문자열/글자) 자료형으로 구분되어 있음.

# 3. 온도가 object로 나온다면 어떤 문제가 생길지 설명
"""
[온도가 object(글자)로 인식될 때 발생하는 문제]
1) 통계 연산 불가:
   - mean(), sum() 같은 수치 계산 함수 적용 시 TypeError가 발생하거나 해당 열이 계산에서 제외됨.
2) 연산자 동작 오류:
   - 숫자 덧셈(+), 크기 비교(>, <) 대신 '문자열 이어붙이기'나 '사전순 비교'가 실행되어 잘못된 결과가 나옴.
3) 시각화 및 모델링 불가:
   - 히스토그램, 선 그래프 등 수치형 차트를 그릴 수 없으며 머신러닝 모델의 입력값으로 사용할 수 없음.
"""
